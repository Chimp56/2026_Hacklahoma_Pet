# App
APP_NAME=Pet API
DEBUG=false
API_V1_PREFIX=/api/v1

# Server
HOST=0.0.0.0
PORT=8000

# Database (PostgreSQL with asyncpg only; use postgresql+asyncpg:// so SQLAlchemy never uses psycopg2)
DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/pet_db
# Set to true if your DB uses a self-signed cert (e.g. some managed Postgres) to skip SSL cert verification
# DATABASE_SSL_NO_VERIFY=true

# Security
SECRET_KEY=your-secret-key-change-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
# When set (e.g. 1), all authenticated users see this user's data. Use after running scripts/seed_mock_data.py.
# DEMO_USER_ID=1

# Seed script (scripts/seed_mock_data.py): demo account and optional Luna user password
# SEED_DEMO_PASSWORD=password123
# SEED_LUNA_PASSWORD=password123

# AI: Gemini (https://aistudio.google.com/apikey). On rate limit, rotates to GEMINI_API_KEY2, then GEMINI_API_KEY3.
GEMINI_API_KEY=
# Optional: extra keys for rotation when rate limited
# GEMINI_API_KEY2=
# GEMINI_API_KEY3=
GEMINI_MODEL=gemini-2.5-flash-lite

# AI: Llama (text-only; Transformers pipeline, local)
HF_TOKEN=
HF_LLAMA_MODEL=meta-llama/Llama-3.2-1B-Instruct
# Llama Vision (image analysis alternative to Gemini; accept license at the model page)
HF_LLAMA_VISION_MODEL=meta-llama/Llama-3.2-11B-Vision-Instruct

# File storage: local | digitalocean
STORAGE_BACKEND=local
STORAGE_LOCAL_PATH=./storage
# DigitalOcean Spaces (S3-compatible). For pet-storage bucket in sfo3 use:
# DO_SPACES_BUCKET=pet-storage
# DO_SPACES_REGION=sfo3
# DO_SPACES_ENDPOINT=https://sfo3.digitaloceanspaces.com
# (Bucket URL will be https://pet-storage.sfo3.digitaloceanspaces.com)
DO_SPACES_KEY=
DO_SPACES_SECRET=
DO_SPACES_REGION=nyc3
DO_SPACES_BUCKET=
DO_SPACES_ENDPOINT=https://nyc3.digitaloceanspaces.com

# Notifications: Slack (incoming webhook)
SLACK_WEBHOOK_URL=
SLACK_DEFAULT_CHANNEL=
# Comma-separated events that trigger Slack: milestone, health_alert, anomaly
NOTIFICATION_FLAG_EVENTS=milestone,health_alert,anomaly

# API public URL (pet share QR links point here, e.g. https://api.quantara.co/share/pet/{id})
API_BASE_URL=https://api.quantara.co
# QR code: base URL for pet profile links (GET /pets/{id}/qr-code returns QR linking to {base}/pet/{id})
PET_PROFILE_BASE_URL=http://localhost:3000
# Optional: qr-code-generator.com API token (if set and no custom logo needed, uses their API)
# QR_CODE_API_ACCESS_TOKEN=

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
